{{!-- <h1>the original post!</h1>
<h3>original comment content: {{selectComment.commentPost}}</h4>
{{log selectComment}}
<form class="updateComment">
    <input type="hidden" id="comment-id" value="{{selectComment.id}}">
    <div class="mb-3 updateComment-textarea" id="blogPost-id" data-blogpostid="{{selectComment.blogPost_id}}">
        <label for="text" class="form-label">Content</label>
        <textarea class="form-control" id="comment-content" rows="3"></textarea>
    </div>
    <button type="submit" class="btn btn-primary">Post!</button>
</form> --}}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card login-card">
                <div class="card-body">
                    <h3>Your Original Comment you'd like to change: {{selectComment.commentPost}}</h3>
                    {{!-- {{log selectComment}} --}}
                    <form class="updateComment">
                        <input type="hidden" id="comment-id" value="{{selectComment.id}}">
                        <div class="mb-3 updateComment-textarea" id="blogPost-id" data-blogpostid="{{selectComment.blogPost_id}}">
                            <label for="text" class="form-label">New Comment</label>
                            <textarea class="form-control" id="comment-content" rows="3"></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary">Post!</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="../js/changeComment.js"></script>