

{{!-- I'm trying  --}}
{{> blogpost-details logged_in=logged_in}}

{{!-- {{#each allPosts as |blogPost| }}
{{> blogpost-details }}
<section class="card">
    <div class="card mb-3" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">{{title}}
        </h5>
        <p class="card-text">{{content}}</p>
        <p class="card-text">Created by: {{ blogPost.user.userName }} <br> On: {{format_date date_created}}</p>
        {{log logged_in}}
        {{#if logged_in}}
            <a href="/blogpost/{{blogPost.id}}" class="btn btn-primary">Comments</a>
        {{else}}
            <a href="/login" class="btn btn-primary">Login to comment</a>
        {{/if}}

    </div>
    </div>
</section>
{{/each}}

<script src="./js/comment.js"></script>
 --}}
